Place your photos in this folder and reference them from the report with a path like:

`photos/your-image.jpg`

The placeholders in the report will keep their layout if you replace them with an `<img>` tag using the same `photo-slot` class.
